<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title><%= username %> - TikTok Video</title>
  <style>
    body {
      background-color: #111;
      color: #fff;
      font-family: sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
    }

    video {
      width: 100%;
      max-width: 600px;
      border: 2px solid #fff;
      border-radius: 8px;
    }

    h1 {
      margin-bottom: 20px;
      font-size: 1.2rem;
      opacity: 0.8;
    }
  </style>
</head>
<body>
  <h1>@<%= username %></h1>
  <video controls autoplay>
    <source src="<%= videoUrl %>" type="video/mp4">
    Your browser does not support video playback.
  </video>
</body>
</html>
