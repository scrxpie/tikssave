<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= videoData.title %> - TikTok Video</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/style.css"> <!-- varsa stil -->
</head>
<body>
  <div class="container">
    <h1><%= videoData.title %></h1>
    <p>by @<%= videoData.username %></p>
    <img src="<%= videoData.cover %>" alt="Thumbnail" style="max-width: 100%; height: auto; border-radius: 10px;">
    
    <div style="margin-top: 20px;">
      <a href="/proxy-download?url=<%= videoData.hdplay || videoData.play %>&type=video&username=<%= videoData.username %>" class="btn">Download MP4</a>
      <a href="/proxy-download?url=<%= videoData.music %>&type=music&username=<%= videoData.username %>" class="btn">Download MP3</a>
    </div>
  </div>
</body>
</html>
