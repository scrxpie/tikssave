<!DOCTYPE html>
<html>
<head>
  <title>Admin Dashboard</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; background: #f5f5f5; }
    h1 { color: #333; }
    .stats { margin-bottom: 20px; }
    .stats p { font-size: 18px; }
    table { width: 100%; border-collapse: collapse; background: #fff; }
    th, td { border: 1px solid #ccc; padding: 10px; text-align: left; font-size: 14px; }
    th { background: #eee; }
  </style>
</head>
<body>
  <h1>Admin Dashboard</h1>

  <div class="stats">
    <p>🧮 <strong>Total Visits:</strong> <%= total %></p>
    <p>📅 <strong>Today's Visits:</strong> <%= today %></p>
    <p>📱 <strong>Unique Visitors (IP):</strong> <%= totalUnique %></p>
    <p>📥 <strong>Total Downloads:</strong> <%= totalDownloads %></p>
  </div>

  <h2>Recent Visits (last 20)</h2>
  <table>
    <thead>
      <tr>
        <th>IP</th>
        <th>Date</th>
        <th>User Agent</th>
      </tr>
    </thead>
    <tbody>
      <% visits.forEach(v => { %>
        <tr>
          <td><%= v.ip %></td>
          <td><%= v.createdAt.toLocaleString() %></td>
          <td><%= v.userAgent %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
