<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>TikTok Downloader</title>
  <style>
    body {
      background: #111;
      color: #eee;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      padding: 0;
    }

    h1 {
      margin-bottom: 0.5rem;
      font-size: 2rem;
      color: #00bcd4;
    }

    p {
      margin-top: 0;
      font-size: 0.95rem;
      color: #888;
    }

    .visitor-count {
      margin-top: 10px;
      font-size: 0.9rem;
      color: #666;
    }

    form {
      margin-top: 2rem;
      display: flex;
      gap: 8px;
      width: 90%;
      max-width: 500px;
    }

    input[type="text"] {
      flex: 1;
      padding: 10px;
      font-size: 1rem;
      border: none;
      border-radius: 6px;
      background: #222;
      color: #fff;
      outline: none;
    }

    button {
      background: #00bcd4;
      color: #000;
      padding: 10px 16px;
      border: none;
      border-radius: 6px;
      font-weight: bold;
      cursor: pointer;
    }

    button:hover {
      background: #009db1;
    }

    footer {
      margin-top: auto;
      font-size: 0.8rem;
      color: #555;
      padding: 1rem;
    }
  </style>
</head>
<body>
  <h1>TikTok Downloader</h1>
  <p>Download videos without watermark</p>

  <p class="visitor-count">Total Visitors: <%= count %></p>

  <form action="/download" method="POST">
    <input type="text" name="url" placeholder="Paste TikTok video URL..." required />
    <button type="submit">Download</button>
  </form>

  <footer>© 2025 TikTok Downloader</footer>
</body>
</html>
