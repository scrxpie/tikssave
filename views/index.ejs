<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>TikTok Downloader</title>
  <style>
    :root {
      --bg: #1e1e1e;
      --text: #e0e0e0;
      --gray: #aaa;
      --blue: #3b82f6;
      --blue-dark: #2563eb;
    }

    * { box-sizing: border-box; }

    body {
      background-color: var(--bg);
      color: var(--text);
      font-family: 'Segoe UI', sans-serif;
      margin: 0;
      padding: 20px 15px 60px;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }

    h1 {
      font-size: 2.5rem;
      color: var(--blue);
      font-weight: 800;
      margin-bottom: 10px;
    }

    .visitors {
      font-size: 0.95rem;
      color: var(--gray);
      margin-bottom: 25px;
    }

    form {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      width: 100%;
      max-width: 600px;
      margin-bottom: 20px;
    }

    input[type="text"] {
      flex: 1;
      padding: 14px;
      font-size: 1rem;
      border: none;
      border-radius: 10px;
      background: #333;
      color: #fff;
    }

    button {
      padding: 14px 18px;
      font-size: 1rem;
      border: none;
      border-radius: 10px;
      font-weight: bold;
      cursor: pointer;
      transition: 0.3s;
    }

    .paste-btn {
      background-color: #666;
      color: #fff;
    }

    .clear-btn {
      background-color: #555;
      color: #fff;
    }

    .fetch-btn {
      background-color: var(--blue);
      color: #fff;
    }

    .fetch-btn:hover {
      background-color: var(--blue-dark);
    }

    #status {
      margin-top: 20px;
      color: var(--gray);
      font-size: 1rem;
      display: none;
    }

    #notify {
      background-color: #333;
      color: #fff;
      padding: 12px 18px;
      border-radius: 10px;
      margin-bottom: 15px;
      display: none;
      box-shadow: 0 0 8px #000;
    }

    #options {
      display: none;
      flex-direction: column;
      gap: 12px;
      width: 100%;
      max-width: 600px;
      margin-bottom: 40px;
    }

    .download-btn {
      background-color: #1e40af;
      color: #fff;
      padding: 14px;
      font-size: 1rem;
      border-radius: 10px;
      cursor: pointer;
      transition: 0.3s;
    }

    .download-btn:hover {
      background-color: var(--blue);
    }

    #about {
      max-width: 600px;
      text-align: left;
      color: var(--gray);
      margin-top: 30px;
      line-height: 1.6;
    }

    #about h2 {
      color: var(--blue);
      font-size: 1.4rem;
      margin-bottom: 10px;
    }

    .accordion {
      margin-top: 30px;
      max-width: 600px;
      width: 100%;
    }

    .accordion-item {
      background: #2a2a2a;
      border-radius: 8px;
      margin-bottom: 10px;
      overflow: hidden;
    }

    .accordion-header {
      padding: 15px 18px;
      cursor: pointer;
      background: #333;
      font-weight: bold;
      color: var(--blue);
      display: flex;
      justify-content: space-between;
    }

    .accordion-header:hover {
      background: var(--blue);
      color: #fff;
    }

    .accordion-content {
      padding: 15px 18px;
      display: none;
      background: #1f1f1f;
      color: #ccc;
    }

    .footer {
      text-align: center;
      margin-top: auto;
      padding: 20px;
      font-size: 0.9rem;
      color: var(--gray);
    }

    @media (max-width: 480px) {
      h1 { font-size: 2rem; }
      input, button { font-size: 1rem; }
    }
  </style>
</head>
<body>
  <h1>TikTok Downloader</h1>
  <p class="visitors">Total Visitors: <%= count %></p>

  <div id="notify">Link pasted! ✅</div>

  <form onsubmit="return false;">
    <input type="text" id="videoUrl" placeholder="Paste TikTok link..." required oninput="togglePasteClear()" />
    <button type="button" id="pasteClearBtn" class="paste-btn" onclick="pasteLink()">Paste</button>
    <button type="button" class="fetch-btn" onclick="fetchLinks()">Download</button>
  </form>

  <div id="status">Fetching download links...</div>
  <div id="options"></div>

  <section id="about">
    <h2>Unlimited TikTok Downloads</h2>
    <p>Download unlimited TikTok videos without watermark in high-quality MP4 or MP3. No registration required.</p>

    <h2>MP4 & MP3 Formats</h2>
    <p>Convert any TikTok to video or audio in one click — totally free.</p>
  </section>

  <section class="accordion">
    <div class="accordion-item">
      <div class="accordion-header" onclick="toggleAccordion(this)">How to copy TikTok link? <span>▶</span></div>
      <div class="accordion-content">Open TikTok, tap Share → Copy Link.</div>
    </div>
    <div class="accordion-item">
      <div class="accordion-header" onclick="toggleAccordion(this)">Why is HD not available? <span>▶</span></div>
      <div class="accordion-content">Not all videos have HD formats. TikTok limits some qualities.</div>
    </div>
    <div class="accordion-item">
      <div class="accordion-header" onclick="toggleAccordion(this)">Is this free? <span>▶</span></div>
      <div class="accordion-content">Yes. It’s 100% free and safe. We don’t store your data.</div>
    </div>
  </section>

  <div class="footer">© <%= new Date().getFullYear() %> tikssave.com</div>

  <script>
    function pasteLink() {
      navigator.clipboard.readText().then(text => {
        document.getElementById('videoUrl').value = text;
        document.getElementById('notify').style.display = 'block';
        togglePasteClear();
        setTimeout(() => {
          document.getElementById('notify').style.display = 'none';
        }, 2500);
      });
    }

    function togglePasteClear() {
      const input = document.getElementById('videoUrl');
      const btn = document.getElementById('pasteClearBtn');
      if (input.value.trim()) {
        btn.textContent = "Clear";
        btn.className = "clear-btn";
        btn.onclick = () => {
          input.value = '';
          togglePasteClear();
        };
      } else {
        btn.textContent = "Paste";
        btn.className = "paste-btn";
        btn.onclick = pasteLink;
      }
    }

    async function fetchLinks() {
      const url = document.getElementById('videoUrl').value.trim();
      if (!url) return alert("Please enter a TikTok URL.");
      document.getElementById("status").style.display = "block";
      document.getElementById("options").style.display = "none";

      try {
        const res = await fetch('/get-links', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ url })
        });

        const data = await res.json();
        document.getElementById("status").style.display = "none";

        if (!data.success) return alert(data.message);

        window.downloadData = data;
        const container = document.getElementById('options');
        container.innerHTML = '';

        if (data.music)
          container.innerHTML += `<button class="download-btn" onclick="download('music')">🎵 MP3 (Audio Only)</button>`;
        if (data.play)
          container.innerHTML += `<button class="download-btn" onclick="download('play')">📽️ SD Video</button>`;
        if (data.hdplay)
          container.innerHTML += `<button class="download-btn" onclick="download('hdplay')">📽️ HD Video</button>`;

        container.style.display = 'flex';
      } catch (err) {
        document.getElementById("status").style.display = "none";
        alert("Error fetching download links.");
        console.error(err);
      }
    }

    function download(type) {
      const data = window.downloadData;
      if (!data[type]) return alert("This quality is not available.");
      const link = document.createElement('a');
      link.href = `/proxy-download?url=${encodeURIComponent(data[type])}&username=${data.username}&type=${type}`;
      link.setAttribute('download', '');
      link.click();
    }

    function toggleAccordion(el) {
      const content = el.nextElementSibling;
      const arrow = el.querySelector("span");
      if (content.style.display === "block") {
        content.style.display = "none";
        arrow.textContent = "▶";
      } else {
        document.querySelectorAll('.accordion-content').forEach(c => c.style.display = "none");
        document.querySelectorAll('.accordion-header span').forEach(a => a.textContent = "▶");
        content.style.display = "block";
        arrow.textContent = "▼";
      }
    }
  </script>
</body>
</html>
