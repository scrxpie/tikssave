<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>TikTok Video Downloader</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      background: linear-gradient(135deg, #1e1e1e, #282828);
      color: #eee;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
      align-items: center;
      justify-content: center;
    }
    header {
      text-align: center;
      margin-bottom: 2rem;
    }
    header h1 {
      font-size: 2.8rem;
      margin-bottom: 0.3rem;
      color: #00c8ff;
      letter-spacing: 2px;
    }
    header p {
      color: #999;
      font-size: 1rem;
      margin-top: 0;
    }
    .visitor-count {
      margin-top: 1rem;
      font-weight: 500;
      font-size: 1.1rem;
      color: #a0a0a0;
    }
    form {
      display: flex;
      gap: 0.5rem;
      max-width: 480px;
      width: 90vw;
      margin-top: 1.5rem;
    }
    input[type="text"] {
      flex: 1;
      padding: 0.75rem 1rem;
      font-size: 1.1rem;
      border: none;
      border-radius: 8px;
      outline: none;
      background: #1e1e1e;
      color: #eee;
      box-shadow: 0 0 6px rgba(0, 200, 255, 0.3);
      transition: background 0.3s;
    }
    input[type="text"]:focus {
      background: #272727;
      box-shadow: 0 0 10px #00c8ff;
    }
    button {
      background: #00c8ff;
      border: none;
      border-radius: 8px;
      color: #121212;
      font-weight: 700;
      font-size: 1.1rem;
      padding: 0 20px;
      cursor: pointer;
      transition: background 0.3s;
      display: flex;
      align-items: center;
      gap: 8px;
      user-select: none;
    }
    button:hover {
      background: #00a6d9;
    }
    button:active {
      background: #0089b5;
    }
    #pasteBtn {
      background: #ffaa00;
      color: #121212;
      padding: 0 15px;
      font-weight: 600;
      border-radius: 8px;
      transition: background 0.3s;
    }
    #pasteBtn:hover {
      background: #cc8800;
    }
    #pasteBtn:active {
      background: #aa6600;
    }
    footer {
      margin-top: auto;
      padding: 1rem;
      font-size: 0.9rem;
      color: #555;
      text-align: center;
    }
    @media (max-width: 520px) {
      form {
        flex-direction: column;
      }
      button, #pasteBtn {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>TikTok Video Downloader</h1>
    <p>Easily download videos without watermark!</p>
  </header>

  <p class="visitor-count">Total Visitors: <%= count %></p>

  <form id="downloadForm" action="/download" method="POST" autocomplete="off">
    <input
      type="text"
      id="videoUrl"
      name="url"
      placeholder="Paste your TikTok video link here"
      required
      spellcheck="false"
      autocorrect="off"
      autocomplete="off"
      aria-label="TikTok video URL"
    />
    <button type="button" id="pasteBtn" title="Paste from Clipboard">📋 Paste</button>
    <button type="submit">Download</button>
  </form>

  <footer>
    © 2025 TikTok Video Downloader | Developer: Asim 
  </footer>

  <script>
    // Paste button copies clipboard content into input field
    document.getElementById('pasteBtn').addEventListener('click', async () => {
      try {
        const text = await navigator.clipboard.readText();
        if (text) {
          document.getElementById('videoUrl').value = text;
          document.getElementById('videoUrl').focus();
        } else {
          alert('Clipboard is empty.');
        }
      } catch (err) {
        alert('Clipboard access denied or not supported.');
      }
    });
  </script>
</body>
</html>
